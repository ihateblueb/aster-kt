import Compose from "../Compose.tsx";

function ComposeWidget() {
    return (
        <div className={`widget-compose widget padded`}>
            <Compose/>
        </div>
    )
}

export default ComposeWidget;
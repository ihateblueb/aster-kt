package site.remlit.aster.exception

/**
 * The relationship between users involved conflict and don't allow the current action to continue.
 *
 * @since 2025.9.1.1-SNAPSHOT
 * */
class RelationshipConflictException(message: String) : Exception(message)
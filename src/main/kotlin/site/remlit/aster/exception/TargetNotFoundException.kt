package site.remlit.aster.exception

/**
 * Exception happening when the target of an interaction cannot be found.
 *
 * @since 2025.9.1.1-SNAPSHOT
 * */
class TargetNotFoundException(message: String) : Exception(message)
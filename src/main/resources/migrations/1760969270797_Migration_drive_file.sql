CREATE TABLE IF NOT EXISTS drive_file (id VARCHAR(125) PRIMARY KEY, "type" VARCHAR(125) NOT NULL, src VARCHAR(5000) NOT NULL, alt VARCHAR(10000) NOT NULL, "sensitive" BOOLEAN DEFAULT FALSE NOT NULL, "user" VARCHAR(125) NOT NULL, "createdAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, "updatedAt" TIMESTAMP NULL, CONSTRAINT fk_drive_file_user__id FOREIGN KEY ("user") REFERENCES "user"(id) ON DELETE CASCADE ON UPDATE RESTRICT);
ALTER TABLE drive_file ADD CONSTRAINT unique_drive_file_id UNIQUE (id);

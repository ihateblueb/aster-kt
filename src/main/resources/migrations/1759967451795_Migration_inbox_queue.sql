CREATE TABLE IF NOT EXISTS inbox_queue (id VARCHAR(125) PRIMARY KEY, status INT NOT NULL, "content" bytea NOT NULL, sender VARCHAR(125) NULL, "createdAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, retries INT NOT NULL, CONSTRAINT fk_inbox_queue_sender__id FOREIGN KEY (sender) REFERENCES "user"(id) ON DELETE CASCADE ON UPDATE RESTRICT);
ALTER TABLE inbox_queue ADD CONSTRAINT unique_inbox_queue_id UNIQUE (id);
